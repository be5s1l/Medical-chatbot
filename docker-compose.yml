services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      CHROMA_DB_PATH: /app/data/chroma_db
    volumes:
      - chroma_data:/app/data/chroma_db

volumes:
  chroma_data: {}
