# Core API
fastapi==0.110.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9

# Validation / settings
pydantic>=2.11.0
pydantic-settings>=2.7.0
python-dotenv==1.0.1

# Logging
loguru==0.7.2

# LLM (Groq)
langchain-core>=0.3.0
langchain-groq>=0.2.0

# Vision (Gemini) — used ONLY for image description
google-ai-generativelanguage>=0.6.0

# OCR
PyPDF2==3.0.1
pdfplumber>=0.11.0
# Optional: `pytesseract` pulls in `pandas` which can be blocked by Windows Application Control.
# We call the Tesseract CLI directly, so keep this optional.
# pytesseract==0.3.10
Pillow>=11.0.0

# Vision (optional in v1; Windows/Python version sensitive). Install later if needed:
# torch
# torchvision
# torchxrayvision

# Testing
pytest==8.2.0
pytest-asyncio==0.23.0
httpx==0.27.0

# Frontend (Streamlit) (un-pinned for NumPy/Python compatibility)
streamlit>=1.41.0

