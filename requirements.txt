# Core API
fastapi==0.110.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9

# Validation / settings
pydantic>=2.11.0
pydantic-settings>=2.7.0
python-dotenv==1.0.1

# Logging
loguru==0.7.2

# RAG / LLM orchestration (un-pinned for Python 3.13 compatibility)
langchain>=0.3.0
langchain-community>=0.3.0
langchain-huggingface>=0.1.0
openai>=1.30.0
langchain-openai>=0.2.0
langchain-ollama>=0.2.0
langchain-google-genai>=2.0.0
langchain-groq>=0.2.0

# NLP (HuggingFace)
transformers>=4.40.0

# Embeddings / vector DB (un-pinned for Python 3.13 compatibility)
sentence-transformers>=3.0.0
chromadb>=0.5.0
# Optional fallback (Windows/Python versions vary). Install only if needed:
# faiss-cpu

# OCR
PyPDF2==3.0.1
# Optional: `pytesseract` pulls in `pandas` which can be blocked by Windows Application Control.
# We call the Tesseract CLI directly, so keep this optional.
# pytesseract==0.3.10
Pillow>=11.0.0

# Vision (optional in v1; Windows/Python version sensitive). Install later if needed:
# torch
# torchvision
# torchxrayvision

# Testing
pytest==8.2.0
pytest-asyncio==0.23.0
httpx==0.27.0

# Frontend (Streamlit) (un-pinned for NumPy/Python compatibility)
streamlit>=1.41.0

